# VintageVision Environment Variables
# Generated: 2025-10-24
# Copy this to .env and fill in the values

# Project Identity
PROJECT_NAME=vintagevision
DOMAIN=vintagevision.app

# Database Configuration
DB_PASSWORD=
# Generate with: openssl rand -base64 32 | tr -d "=+/" | cut -c1-32

# MinIO Configuration (S3-Compatible Storage)
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
# Generate with: openssl rand -base64 32 | tr -d "=+/" | cut -c1-32

# Session Configuration
SESSION_SECRET=
# Generate with: openssl rand -base64 32 | tr -d "=+/" | cut -c1-32

# OpenAI API
OPENAI_API_KEY=

# Google OAuth (Self-Hosted Authentication)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: https://vintagevision.app/api/auth/google/callback

# Environment
NODE_ENV=production
TZ=UTC
